# API 서버 베이스 URL. 엔드포인트는 이 뒤에 /auth/login, /auth/signup, /auth/refresh 로 붙는다.
# 백엔드 서버가 아직 배포 전(NOT_WORKING)이므로 실제 주소가 확정되면 교체할 것.
VITE_API_BASE_URL=https://localquest.dsmhs.kr
# 목데이터 모드. true(기본)면 서버 없이 auth가 목데이터로 동작. 실제 연동 시 false.
VITE_USE_MOCK=true
